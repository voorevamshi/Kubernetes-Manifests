apiVersion: v1
kind: Pod
metadata:
  name: test
spec:
  containers:
    - name: webapp
      image: nginx
      ports:
        - containerPort: 80
    - name: tom
      image: tomcat
      ports:
        - containerPort: 8080
