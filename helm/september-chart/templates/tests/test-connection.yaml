apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "september-chart.fullname" . }}-test-connection"
  labels:
    {{- include "september-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "september-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
