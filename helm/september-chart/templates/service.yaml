apiVersion: v1
kind: Service
metadata:
  name: {{ include "september-chart.fullname" . }}
  labels:
    {{- include "september-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "september-chart.selectorLabels" . | nindent 4 }}
