apiVersion: v1
kind: Pod
metadata:
  name: webapp
  labels:
    type: frontend
spec:
  containers:
    - name: app
      image: nginx
      ports:
        - containerPort: 80
      resources:
        requests:
          cpu: 100m
        limits:
          cpu: 1