apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  {{- with .Values.mysqlPvc }}
  name: {{ .name }}
  labels:
{{- include "mysql.labels" . | indent 4 }}
spec:
  storageClassName: {{ .scName }}
  accessModes:
    - {{ .accessMode }}
  resources:
    requests:
      storage: {{ .capacity }}
  {{- end }}